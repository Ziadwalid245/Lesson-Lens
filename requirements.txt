# Only the packages Lesson Lens imports directly; pip pulls in the rest.
# Save this file as UTF-8 (PowerShell 5's ">" writes UTF-16).
# On Windows the plain PyPI torch wheel is already CPU-only.

faster-whisper==1.2.1
silero-vad==6.2.3
torch==2.12.1
numpy==2.4.6
scipy==1.18.0
sounddevice==0.5.6
PyAudioWPatch==0.2.12.8
python-docx==1.2.0
pydantic==2.13.5
requests==2.34.2
platformdirs==4.11.12
PySide6-Essentials==6.11.2

# build tool
pyinstaller==6.22.3
